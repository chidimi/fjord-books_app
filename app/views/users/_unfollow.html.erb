<%= form_with(model: current_user.active_relationships.find_by(followee_id: @user.id),
            html: { method: :delete }, local: false) do |f| %>
<%= f.submit t('views.common.Unfollow'), class: "btn" %>
<% end %>