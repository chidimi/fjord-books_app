<%= form_with(model: current_user.active_relationships.build, local: false) do |f| %>
<div><%= hidden_field_tag :followee_id, @user.id %></div>
<%= f.submit t('views.common.Follow'), class: "btn btn-primary" %>
<% end %>